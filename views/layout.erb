<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title><%= @title %></title>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/">DC's Profile</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li <%= 'class="active"' if @title.downcase == "home" %>><a href="/">Home</a></li>
            <li <%= 'class="active"' if @title.downcase == "about" %>><a href="/about">About</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li <%= 'class="active"' if @title.downcase == "contact" %>><a href="/contact">Contact</a></li>
          </ul>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
